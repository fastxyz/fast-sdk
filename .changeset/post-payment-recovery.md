---
"@fastxyz/allset-sdk": minor
---

Expose `PostPaymentRecoveryError` for failures after a Fast transaction has succeeded, preserving transaction identities and recovery envelopes. Require an explicit positive relayer acknowledgement before reporting `executeIntent` success.
